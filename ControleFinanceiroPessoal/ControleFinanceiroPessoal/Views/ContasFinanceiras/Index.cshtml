@model IEnumerable<ControleFinanceiroPessoal.Models.ContaFinanceira>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoConta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SaldoAtual)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NomeInstituicao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoConta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SaldoAtual)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomeInstituicao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdContaFinanceira">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdContaFinanceira">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdContaFinanceira">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
